'use client';

import React from 'react';
// import DashboardPage from '@/app/(routes)/dashboard/page';

// Demo wrapper that bypasses authentication requirement - temporarily disabled
export default function DemoPage() {
  return <div>Demo page temporarily disabled during build fix</div>;
}